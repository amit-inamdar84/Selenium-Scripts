package seleniumTutorial;

import org.openqa.selenium.chrome.ChromeDriverService;
import org.openqa.selenium.chrome.ChromeOptions;

public class SeleniumWebdriverBasics {
	//Selenium webdriver components - Language level bindings, Selenium Webdriver API and Drivers.
	//We send instructions from language to selenium API library which in turn sends to respective driver.
	//ChromeDriver class extends RemoteWebDriver and implements LocationContext, WebStorage, HasTouchScreen, NetworkConnection and indirectly implements
	//WebDriver, SearchContext, JavascriptExecutor etc
	//Constructors of ChromeDriver class: ChromeDriver(), ChromeDriver(ChromeDriverService service), ChromeDriver(ChromeDriverService service, ChromeOptions options)
	//ChromeDriver(ChromeOptions options)
	
}
